## Logging Middleware
here is the most important step is to give priority to the 
privacy data

so we are storing the data into the .env file and then we add file into gitignore to do not push into the 
git hub 

## steps to use .env values properly
npm i dotenv
and config into server.js now we can acces them by  
process.env.name_declared in the .env file

## adding steps into the .gitignore
this prevents to add sensitive data into the git hub platform

## what i have implemeted
 registration logging middlweare and auth
## steps to fork it
   download my zip and extract
   after extracting